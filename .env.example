# Copy this file to .env and fill in real local values.
# .env is gitignored — never commit real secrets.

# --- Postgres ---
POSTGRES_USER=retaillake
POSTGRES_PASSWORD=changeme
POSTGRES_DB=retaildb

# --- MinIO ---
MINIO_ROOT_USER=retaillake
MINIO_ROOT_PASSWORD=changeme123
MINIO_ENDPOINT=http://localhost:9000
MINIO_CONSOLE=http://localhost:9001

# --- Airflow ---
# Generate a real Fernet key with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=replace-with-generated-fernet-key
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=changeme

# --- Mock data generator ---
SEED_RANDOM_SEED=42

SUPERSET_SECRET_KEY=50691b8587a50d6c26492352a4ad0bd1b6fc9fb39efa641021b2883c2ccbc9fe